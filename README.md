# Incident Response Team using Google's ADK

## Capabilities
- uses `ParallelAgents` to create subtasks required for a incident response team (logs, metrics & recent git commits analysis)
- `SequentialAgent` to analyse all the data obtained by the subagents. Correlate and judge the root-cause (with confidence scores).


